{% extends 'layouts/userdefault.html.twig'%}
{% block main %}
<div class="container mt-4 bg-light rounded p-4 text-center">
    <h2>Gérer les biens</h2>
    <table class="table w-75 mt-4">
        <tbody>
            <tr>
                <td scope="col">Titre</td>
                <td scope="col">Action</td>
            </tr>
            {% for list_announcement in list_announcements %}
                <tr>
                    <td scope="rox"><a href="#">{{ list_announcement.title}}</a></td>
                    <td><a href="{{ path('luxo_edit_show', {id:list_announcement.id}) }}" class ="btn btn-secondary">Modifier</a>
                    <a href="#" class="btn btn-danger">Supprimer</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('luxo_newannouncement_show') }}" class="d-inline">Crée une annonce</a>
</div>
{% endblock %}
