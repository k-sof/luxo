{% extends 'layouts/default.html.twig' %}
{% block main %}
    <div class="container mt-4 bg-light rounded p-4">
        {{ form_start(form) }}
            {% if message %}
                <div class="alert alert-success" role="alert">{{ message }}</div>
            {% endif %}
            <h1>Connecte-vous</h1>
            {{ form_widget(form) }}
            <div class="row justify-content-center">
                <div class="col-md-4"><a href="{{ path('luxo_demande_show') }}">oublier le mot de passe</a></div>
            </div>
        <button class="btn btn-primary">Connection</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
